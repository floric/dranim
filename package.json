{
  "name": "masterthesis",
  "private": true,
  "license": "MIT",
  "version": "1.0.0",
  "engines": {
    "node": ">= 6.11.1",
    "yarn": ">= 1.0.0"
  },
  "scripts": {
    "build": "lerna run build --stream",
    "start": "lerna run start --stream",
    "test": "lerna run test --stream",
    "lint": "lerna run lint --stream",
    "clean": "lerna run clean --stream && rm -rf node_modules && sudo rm -rf data",
    "mongodb": "sudo docker run -v /home/florian/Dev/Github/masterthesis/configs:/etc/mongo -p 127.0.0.1:27017:27017 mongo:latest --config /etc/mongo/mongodb.yml"
  },
  "author": {
    "name": "Florian Richter"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "prettier": {
    "singleQuote": true
  },
  "devDependencies": {
    "lerna": "^3.1.4",
    "tslint": "^5.2.0",
    "tslint-config-prettier": "^1.13.0",
    "tslint-plugin-prettier": "^1.3.0",
    "typescript": "^3.0.1"
  },
  "dependencies": {
    "@types/node": "10.7.1"
  }
}
