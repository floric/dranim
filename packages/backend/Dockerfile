FROM node:slim

WORKDIR /
COPY dist .
ENV NODE_ENV="production"
ENV BACKEND_DOMAIN $BACKEND_DOMAIN
ENV FRONTEND_DOMAIN $FRONTEND_DOMAIN
ENV DB_USER $DB_USER
ENV DB_PW $DB_PW
EXPOSE 80

CMD ["node", "main.js"]
